[pytest]
# Keep build output off the collection path. dist_app/ and build_pyi/ are
# PyInstaller artifacts (gitignored) that bundle whole site-packages trees;
# collecting them fails on unrelated third-party imports and has nothing to do
# with this project's tests.
norecursedirs = dist_app build_pyi build dist .git node_modules *.egg .*
