# Build artifacts (rebuildable from source)
spike/R50_Spike_Listener.exe
spike/build/
spike/dist/
*.spec

# PyInstaller outputs (catcher exe — rebuildable from source)
/build/
/dist/

# Python
__pycache__/
*.pyc
.venv/
venv/
.venv311/
.env

# Spike capture output (raw shot data / device IDs / can be large)
spike/spike_log_*.jsonl
spike/spike_startup_error.txt

# Catcher/vision runtime output (per-run, not source)
data/pending_shots.jsonl
data/*.db
data/*.db-wal
data/*.db-shm
swings/

# Pose model files (downloaded, not source)
*.task
*.tflite

# Sample media (not versioned)
*.MOV
*.mov
*.mp4

# Pro-reference build scratch (CC BY-NC GolfDB clone + downloaded videos +
# annotation table + per-run cache). NEVER commit these; only the derived
# coach/norms/pro_reference/pro_reference.json is versioned.
.proref_work/

# OS cruft
Thumbs.db
.DS_Store
desktop.ini

# Runtime server log (dev)
.uvicorn_8000.log

.superpowers/
models/
pose_ab.py
sc_tmp.html

# PyInstaller build outputs
dist_app/
build_pyi/
build/
*.spec.bak
# ...but keep the app's build recipe under version control (overrides *.spec above)
!garagetec.spec

# Impeccable design-critique snapshots (local review scratch)
.impeccable/
